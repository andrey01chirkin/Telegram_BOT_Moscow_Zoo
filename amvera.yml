---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12.5
build:
  requirementsPath: requirements.txt
run:
  command: python -m app.run
  persistenceMount: /data
  containerPort: 80
